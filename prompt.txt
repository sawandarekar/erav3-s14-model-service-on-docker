create python application file for frontend with gradio library which will be used to create a simple application with a sample text input, max token length 1000, and a submit button.
add backend service in appplication which will call another api to get the data.
To create UI for the application, use better ui library.
------------------------------
generate docker compose file with two services text-gen-frontend and text-gen-backend which uses docker images of same. make sure these two services can communicate like bridge